export * from '../syntax';
export * from './cdn';

/** Shared storage / analytics namespace prefix; keep aligned with `WebStorage`'s default package prefix. */
export const unique_web_id = 'likwidmack:web';
