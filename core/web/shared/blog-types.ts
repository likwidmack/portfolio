/**
 * Shared blog post types + slug helper (safe for app + server imports).
 * SoT: `@tgmc/web-layer-admin/shared/blog-types`.
 */
export {
  normalizeSlug,
  type BlogPost,
  type BlogPostStatus,
  type CreateBlogPostInput,
  type UpdateBlogPostInput,
} from '@tgmc/web-layer-admin/shared/blog-types';
