{
  "seo": {
    "title": "Code — Tamara Mack",
    "description": "Workspace repositories and packages — editor aesthetic, repos not bio."
  },
  "hero": {
    "eyebrow": "Code",
    "title": "Repositories",
    "lede": "Editor aesthetic for the monorepo surface — packages and paths, not a biography."
  },
  "windowTitle": "~/code/repos — editor aesthetic, repos not bio",
  "explorer": [
    "portfolio/",
    "packages/utilities",
    "packages/media-player",
    "theme/core",
    "server/utils/ai-lab-security.ts",
    "public/d/js/helix-3.js"
  ],
  "repos": [
    {
      "id": "portfolio",
      "name": "portfolio",
      "language": "TypeScript",
      "description": "Nx + Nuxt 4 monorepo. Theme tokens, SAM, four-env topology.",
      "updated": "2d",
      "href": "#"
    },
    {
      "id": "utilities",
      "name": "utilities",
      "language": "TypeScript",
      "description": "Node / browser / universal entries. No window at module load.",
      "updated": "5d",
      "href": "#"
    },
    {
      "id": "media-player",
      "name": "media-player",
      "language": "Vue",
      "description": "Framework-free leaf player used by gallery exhibits.",
      "updated": "1w",
      "href": "#"
    },
    {
      "id": "theme",
      "name": "theme",
      "language": "SCSS",
      "description": "Packaged design tokens consumed via @tgmc/theme dist.",
      "updated": "3d",
      "href": "#"
    }
  ],
  "samples": [
    {
      "id": "slug",
      "module": "@tgmc/web-layer-admin",
      "file": "shared/blog-types.ts",
      "language": "typescript",
      "style": "Pure function, no I/O",
      "dependencies": "None",
      "usedIn": "Blog admin + the public /blog routes share this so slugs never drift.",
      "description": "Normalizes a post title or user-entered slug into lowercase kebab-case before it is saved or looked up, so the admin layer and the public site always agree on a URL.",
      "source": [
        "// Admin and public /blog share this so a title and a URL never disagree.",
        "export function normalizeSlug(raw: string): string {",
        "  return raw",
        "    .trim()",
        "    .toLowerCase()",
        "    .replace(/\\s+/g, '-') // \"My Post\" → \"my-post\"",
        "    .replace(/[^a-z0-9-]/g, '') // drop punctuation / unicode",
        "    .replace(/-+/g, '-') // collapse leftover hyphen runs",
        "    .replace(/^-|-$/g, ''); // no leading or trailing hyphen",
        "}"
      ]
    },
    {
      "id": "gallery-filter",
      "module": "@tgmc/web",
      "file": "shared/gallery-types.ts",
      "language": "typescript",
      "style": "Pure predicate, exhaustively typed",
      "dependencies": "None",
      "usedIn": "The Gallery page filters every post through this before rendering a tile.",
      "description": "Gallery posts can be filtered by category and by media kind at the same time. This keeps both checks in one place and uses a `never` exhaustiveness check so a new filter kind can't be added without updating the matcher.",
      "source": [
        "// One predicate for category ∩ media kind — every Gallery tile passes through here.",
        "export function filterGalleryPosts(",
        "  posts: GalleryPost[],",
        "  groupId: string,",
        "  kind: GalleryFilterKind",
        "): GalleryPost[] {",
        "  return posts.filter((post) => matchesGroup(post, groupId) && matchesKind(post, kind));",
        "}",
        "",
        "function matchesKind(post: GalleryPost, kind: GalleryFilterKind): boolean {",
        "  switch (kind) {",
        "    case 'all':",
        "      return true;",
        "    case 'code':",
        "      return post.kind === 'code';",
        "    case 'viz':",
        "      return post.kind === 'viz';",
        "    case 'image':",
        "      return (",
        "        post.kind === 'shell' ||",
        "        (post.kind === 'media' && post.exhibit?.kind === 'media' && post.exhibit.mediaType === 'image')",
        "      );",
        "    case 'video':",
        "      return post.kind === 'media' && post.exhibit?.kind === 'media' && post.exhibit.mediaType === 'video';",
        "    default: {",
        "      const _exhaustive: never = kind; // new filter kind → compile error until this switch is updated",
        "      return _exhaustive;",
        "    }",
        "  }",
        "}"
      ]
    },
    {
      "id": "accent-tokens",
      "module": "@tgmc/web",
      "file": "shared/personalization.ts",
      "language": "typescript",
      "style": "Small, deterministic, no side effects",
      "dependencies": "@tgmc/theme (pickContrastingInk)",
      "usedIn": "Backs the accent-color picker in the site’s Personalize dialog.",
      "description": "When a visitor picks an accent color from the Personalize dialog, this turns that choice into the actual CSS custom properties the theme reads, and asks the theme package to pick a readable foreground ink for buttons against that color.",
      "source": [
        "// Personalize dialog → the CSS variables the theme actually reads.",
        "export function buildAccentTokens(next: AccentId): Record<string, string> {",
        "  const preset = ACCENT_PRESETS.find((item) => item.id === next) ?? ACCENT_PRESETS[0]!;",
        "  // Ink is WCAG-picked against the fill, not a hardcoded white/black.",
        "  const ink = pickContrastingInk({ backgroundColor: preset.color });",
        "  return {",
        "    '--primary-color': preset.color,",
        "    '--secondary-color': preset.color,",
        "    '--accent-color': preset.color,",
        "    '--focus-ring': preset.color,",
        "    '--button-fg': ink,",
        "  };",
        "}"
      ]
    },
    {
      "id": "mse-queue",
      "module": "@tgmc/media-player",
      "file": "src/lib/engine/source-buffer-queue.ts",
      "language": "typescript",
      "style": "Serialized async queue over a browser API with one-in-flight constraint",
      "dependencies": "Media Source Extensions (SourceBuffer)",
      "usedIn": "Media Systems — MSE/HLS loaders enqueue fMP4 chunks faster than the browser can append.",
      "description": "MSE allows only one appendBuffer in flight per SourceBuffer. This queue serializes chunks, waits for updateend, and re-enters drain if more data arrived mid-cycle — the same recovery pattern used in the newsroom player work.",
      "source": [
        "// MSE: only one appendBuffer may be in flight per SourceBuffer.",
        "export class SourceBufferQueue {",
        "  private readonly queue: QueuedAppend[] = [];",
        "  private draining = false;",
        "  private destroyed = false;",
        "",
        "  enqueue(kind: BufferKind, data: Uint8Array): void {",
        "    if (this.destroyed) return;",
        "    this.queue.push({ bufferKind: kind, data });",
        "    void this.drain(); // kick; drain re-enters if more chunks arrive mid-cycle",
        "  }",
        "",
        "  async drain(): Promise<void> {",
        "    if (this.draining || this.destroyed) return;",
        "    this.draining = true;",
        "    try {",
        "      while (this.queue.length > 0 && !this.destroyed) {",
        "        const next = this.queue[0];",
        "        const buffer = this.getBuffer(next.bufferKind);",
        "        if (!buffer) throw new Error(`Missing SourceBuffer for ${next.bufferKind}`);",
        "        await this.appendWhenReady(buffer, next.data); // waits for updateend",
        "        this.queue.shift();",
        "      }",
        "    } catch (error) {",
        "      this.onError(error instanceof Error ? error : new Error(String(error)));",
        "    } finally {",
        "      this.draining = false;",
        "      if (this.queue.length > 0 && !this.destroyed) void this.drain(); // catch up",
        "    }",
        "  }",
        "",
        "  private appendWhenReady(buffer: SourceBuffer, data: Uint8Array): Promise<void> {",
        "    return new Promise((resolve, reject) => {",
        "      const onUpdateEnd = () => {",
        "        buffer.removeEventListener('updateend', onUpdateEnd);",
        "        buffer.removeEventListener('error', onError);",
        "        resolve();",
        "      };",
        "      const onError = () => {",
        "        buffer.removeEventListener('updateend', onUpdateEnd);",
        "        buffer.removeEventListener('error', onError);",
        "        reject(new Error('SourceBuffer append failed'));",
        "      };",
        "      buffer.addEventListener('updateend', onUpdateEnd);",
        "      buffer.addEventListener('error', onError);",
        "      try {",
        "        buffer.appendBuffer(data as BufferSource);",
        "      } catch (error) {",
        "        buffer.removeEventListener('updateend', onUpdateEnd);",
        "        buffer.removeEventListener('error', onError);",
        "        reject(error instanceof Error ? error : new Error(String(error)));",
        "      }",
        "    });",
        "  }",
        "}"
      ]
    },
    {
      "id": "ai-continuation",
      "module": "@tgmc/web",
      "file": "server/utils/ai-lab-security.ts",
      "language": "typescript",
      "style": "HMAC-signed, one-time continuation with timing-safe compare",
      "dependencies": "node:crypto (createHmac, timingSafeEqual), h3 cookies",
      "usedIn": "Human-Controlled AI Lab — /plan issues a continuation; /complete burns the jti so the same token cannot replay.",
      "description": "Live lab mode never trusts a raw plan from the browser. The server signs the approved steps, then consumeContinuation verifies the HMAC, rejects expiry, and records the jti on an httpOnly spent cookie so a retry cannot burn quota twice.",
      "source": [
        "// HMAC over the payload — the browser never gets a raw unsigned plan.",
        "function sign(payload: string, secret: string): string {",
        "  return createHmac('sha256', secret).update(payload).digest('base64url');",
        "}",
        "",
        "function safeEqual(left: string, right: string): boolean {",
        "  const a = Buffer.from(left);",
        "  const b = Buffer.from(right);",
        "  return a.length === b.length && timingSafeEqual(a, b); // reject on length, then constant-time compare",
        "}",
        "",
        "function parseSigned<T>(token: string, secret: string): T | null {",
        "  const [payload, signature] = token.split('.');",
        "  if (!payload || !signature || !safeEqual(sign(payload, secret), signature)) return null;",
        "  try {",
        "    return JSON.parse(Buffer.from(payload, 'base64url').toString('utf8')) as T;",
        "  } catch {",
        "    return null;",
        "  }",
        "}",
        "",
        "export function consumeContinuation(",
        "  event: H3Event,",
        "  token: string,",
        "  secret: string,",
        "  now = Date.now()",
        "): Continuation | null {",
        "  const continuation = verifyContinuation(token, secret, now);",
        "  if (!continuation) return null;",
        "",
        "  const spent = readSpent(event, secret, now);",
        "  if (spent.ids.includes(continuation.jti)) return null; // already burned",
        "",
        "  spent.ids = [...spent.ids, continuation.jti].slice(-MAX_SPENT_IDS);",
        "  setSignedCookie(event, SPENT_COOKIE, spent, secret, Math.ceil((spent.exp - now) / 1000)); // httpOnly; /complete cannot replay",
        "  return continuation;",
        "}"
      ]
    },
    {
      "id": "fill-contrast",
      "module": "@tgmc/theme",
      "file": "src/contrast.ts",
      "language": "typescript",
      "style": "WCAG luminance over a composite fill, not a single hex",
      "dependencies": "None (sRGB math only)",
      "usedIn": "Experience systems — button and accent chrome must stay readable against both the solid fill and the gradient layered on top.",
      "description": "On-fill ink is chosen by the worst-case WCAG contrast ratio across the opaque background-color and every parseable gradient stop. That is why Personalize can change the accent without dropping button text below a usable contrast floor.",
      "source": [
        "export function relativeLuminance(rgb: ContrastRgb): number {",
        "  const channel = (value: number): number => {",
        "    const s = value / 255;",
        "    // sRGB → linear, then WCAG relative-luminance weights.",
        "    return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4;",
        "  };",
        "  return 0.2126 * channel(rgb.r) + 0.7152 * channel(rgb.g) + 0.0722 * channel(rgb.b);",
        "}",
        "",
        "export function contrastRatio(foreground: ContrastRgb, background: ContrastRgb): number {",
        "  const L1 = relativeLuminance(foreground);",
        "  const L2 = relativeLuminance(background);",
        "  const lighter = Math.max(L1, L2);",
        "  const darker = Math.min(L1, L2);",
        "  return (lighter + 0.05) / (darker + 0.05); // WCAG contrast formula",
        "}",
        "",
        "function worstContrastRatio(fg: ContrastRgb, backgrounds: ContrastRgb[]): number {",
        "  return Math.min(...backgrounds.map((bg) => contrastRatio(fg, bg))); // weakest stop wins",
        "}",
        "",
        "export function pickContrastingInk(input: FillContrastInput): string {",
        "  const lightInk = input.lightInk ?? ON_FILL_INK_LIGHT;",
        "  const darkInk = input.darkInk ?? ON_FILL_INK_DARK;",
        "  const backgrounds = collectFillBackgrounds(input);",
        "  if (backgrounds.length === 0) return darkInk;",
        "",
        "  const lightFg = parseCssColor(lightInk);",
        "  const darkFg = parseCssColor(darkInk);",
        "  if (!lightFg || !darkFg) return darkInk;",
        "",
        "  // Prefer the ink whose worst-case ratio (solid fill + gradient stops) is better.",
        "  return worstContrastRatio(lightFg, backgrounds) >= worstContrastRatio(darkFg, backgrounds)",
        "    ? lightInk",
        "    : darkInk;",
        "}"
      ]
    },
    {
      "id": "dna-helix",
      "module": "@tgmc/web",
      "file": "public/d/js/helix-3.js",
      "language": "javascript",
      "style": "Parametric 3D construction with groove offset and base-pair geometry",
      "dependencies": "three, EffectComposer, UnrealBloomPass, BokehPass",
      "usedIn": "Innovation prototyping / spatial studies — the public DNA helix demo (glass materials, bloom, chromatic aberration).",
      "description": "Each rung is placed on an asymmetric double helix: strand two is rotated by GROOVE_OFFSET so major/minor grooves appear, then the nitrogenous bases are two cylinders that meet at the midpoint with A–T (2) or C–G (3) hydrogen-bond markers.",
      "source": [
        "for (let i = 0; i < NUM_BASE_PAIRS; i++) {",
        "  const angle = i * ROTATION_PER_BP;",
        "  const yPos = i * VERTICAL_SPACING - (NUM_BASE_PAIRS * VERTICAL_SPACING) / 2;",
        "",
        "  const x1 = Math.cos(angle) * HELIX_RADIUS;",
        "  const z1 = Math.sin(angle) * HELIX_RADIUS;",
        "  // Strand 2 is rotated so major/minor grooves appear (not a 180° ladder).",
        "  const x2 = Math.cos(angle + GROOVE_OFFSET) * HELIX_RADIUS;",
        "  const z2 = Math.sin(angle + GROOVE_OFFSET) * HELIX_RADIUS;",
        "",
        "  strand1Points.push(new THREE.Vector3(x1, yPos, z1));",
        "  strand2Points.push(new THREE.Vector3(x2, yPos, z2));",
        "",
        "  const isATPair = Math.random() > 0.5;",
        "  const midX = (x1 + x2) / 2;",
        "  const midZ = (z1 + z2) / 2;",
        "  const startVec = new THREE.Vector3(x1, yPos, z1);",
        "  const midVec = new THREE.Vector3(midX, yPos, midZ);",
        "  const dist = startVec.distanceTo(midVec);",
        "",
        "  const base1 = new THREE.Mesh(base1Geom, isATPair ? adenineMat : cytosineMat);",
        "  base1.scale.set(1, dist, 1);",
        "  base1.position.copy(startVec.clone().lerp(midVec, 0.5));",
        "  base1.lookAt(midVec);",
        "  base1.rotateX(Math.PI / 2);",
        "  dnaHelixGroup.add(base1);",
        "",
        "  const numBonds = isATPair ? 2 : 3; // A–T two hydrogen bonds; C–G three",
        "  for (let j = 0; j < numBonds; j++) {",
        "    const hydrogenBond = new THREE.Mesh(bondGeometry, bondMaterial);",
        "    const verticalOffset = (j - (numBonds - 1) / 2) * 0.15;",
        "    hydrogenBond.position.set((x1 + x2) / 2, yPos + verticalOffset, (z1 + z2) / 2);",
        "    dnaHelixGroup.add(hydrogenBond);",
        "  }",
        "}"
      ]
    }
  ]
}
